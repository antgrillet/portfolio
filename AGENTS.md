# AGENTS.md

## Project Conventions

- Use `bun` instead of `npm` for all package management, script execution, dependency installation, and lockfile-related workflows in this repository.
- Prefer `bun run <script>` over `npm run <script>`.
- Prefer `bun add`, `bun remove`, and related `bun` commands when modifying dependencies.
